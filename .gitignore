node_modules/
dist/
.env
# Local test env (contains test DB credentials) — never commit; but keep the
# tracked .env.test.example template so the safe values are documented.
.env.test
**/.env.test
!.env.test.example
!**/.env.test.example
.vscode/
.claude/
.DS_Store
__pycache__/
*.pyc
coverage/
backend/node_modules/
frontend/node_modules/
backend/dist/
frontend/dist/
backend/uploads/
backend/fonts/
test-screenshots/
# Operator infra config (secrets / env-specific paths — never commit)
scripts/offhost-backup-sync.env
scripts/restore-test.env

# Deployment-transfer and in-repo backup artifacts
/backups/
*.bundle

# Local Codex planning and generated evidence bundles; never deploy or commit
# raw UAT screenshots, local validator logs, or machine-specific handoff output.
/outputs/
/task_plan.md
/notes.md
