# สถานะงาน — 23 มิถุนายน 2569 (2026-06-23)

**Branch:** `security/audit-fixes-2026-06-18`
**สรุป:** รีวิว+แก้ findings ของ Codex, UAT 6 role, แก้บั๊ก UAT, **refresh ฐาน dev เป็นข้อมูล prod ปัจจุบัน (245 คัน)**, ปรับ UX/UI ทั้งแอป, และ **harmonize งาน Codex (operationsAlert / migration-baseline) เข้าเป็นทีมเดียว**
**สถานะ:** **local เสร็จครบทั้งหมด** ผ่าน build + test + guard ครบ — เหลือแค่ **commit รอบใหม่ (รอพี่สั่ง)**. Production ปกติดี ไม่ต้อง restore

> **สถานะ Production (ยืนยันจริงผ่าน Codex 2026-06-23):** ฐาน `lampang_bus` บน Production **มีอยู่และทำงานปกติ** — 38 ตาราง, schools=121 students=1692 **vehicles=245** (ใหม่กว่า backup ที่มี 210), backend health `success`/`connected`, log **ไม่มี** `Unknown database`
> ⚠️ **ห้าม restore backup ทับ prod เด็ดขาด** — จะทำข้อมูลปัจจุบันหาย (245→210). เหตุการณ์ `Unknown database` เมื่อ 06-22 เป็นเหตุการณ์ **ชั่วคราวที่หายไปเองแล้ว**
> งานจริงที่เหลือคือ **refresh ฐาน dev local ให้ตรง prod ปัจจุบัน** (pull fresh dump → restore local) ไม่ใช่ restore กลับ prod

---

## 1. ทำเสร็จแล้ว ✅

### 1.1 รีวิว + แก้ findings (จากงาน Codex)
- ✅ safety-policy: ตรวจ `license_expiry` (ใบอนุญาตหมดอายุ → BLOCK), `SUSPENDED` hard-block จาก status, ORDER BY ให้ตรงกัน
- ✅ driver.routes: emergency รายงานได้แม้ยังไม่เริ่มรอบ (shift-independent fallback), ไม่ audit ทุก GPS ping, ลบ dead audit-catch, รวม shifts/start
- ✅ QR สาธารณะ: ใบตรวจหมดอายุไม่ขึ้นเขียว (false-green) + โชว์ eligibility/เอกสาร
- ✅ env: `process.exit` ไม่ kill worker ตอน test, ระบุ `SAFETY_ENFORCEMENT_AT` เป็น metadata
- ✅ test isolation: `assertDisposableTestDatabase` + `.env.test` loader + `dbHelper` + guard ทุกไฟล์เทสต์ — Jest ทำลาย `lampang_bus_dev` ไม่ได้
- ✅ Readiness layer: service + route + UI (aggregate, ไม่มี PII, ไม่ false-green) + startup probe (กัน flag เปิดโดยไม่มี migration 039)

### 1.2 บั๊กจาก UAT (6 role) — แก้ + verify live แล้ว
- ✅ `GET /api/admin/audit-logs?format=csv` → 500 เป็น 200 (แก้ TDZ ที่ admin.routes.js)
- ✅ `/api/affiliation/students` ตัด `parent_name`/`parent_phone` ออก (ระดับเขตไม่ควรเห็น PDPA) — school ยังมีปกติ
- ✅ `GET /api/transport/vehicles/pending` + `/expiring` → 200 (เดิม 404 ชนกับ `/:id`)
- ✅ `GET /api/auth/me` คืน `driver_id`
- ✅ CLAUDE.md §5.4–5.6 sync ตรง path จริง (`/api/affiliation`, `/api/province`) + §5.8 (`/api/reports/*`)
- 🧹 ลบไฟล์ `backend/uat_token.txt` ที่หลุด (ไม่ commit token)

### 1.3 UX/UI (อิง impeccable / DESIGN.md)
- ✅ Login redesign (on design token + scale-in + focus ring + spinner + error semantic)
- ✅ Motion primitives: `fade-in`, `scale-in`, `.stagger-children`
- ✅ Page transition ทั้งแอป (fade-in ตอนเปลี่ยน route) + KPIGrid stagger (ทุก dashboard)
- ✅ AppCard `interactive`, LoadingState/ErrorState fade-in
- ✅ DeploymentReadiness: แก้คลาส tailwind ที่ไม่มีจริง (`border-line`/`text-accent`/`divide-line`) + AppCard/StatusBadge/progress bar/row hover
- ✅ **Polish 9 หน้า flagship** (6 dashboards: driver/school/affiliation/province/transport/admin + 3 verification: VehicleVerification/VerificationQueue/DriverShift) → raw tailwind → design token, shared components, motion-safe entrance, แก้ DESIGN.md violations
- ✅ **Polish 3 admin pages ของ Codex** (DriverIntegrity/StudentTransferRequests/VehicleRequests) → token + StatusBadge ให้ตรงทีม
- ✅ RiskCard/AttentionCard + แก้ label warnings เดิม 7 จุด

### 1.4 ผลทดสอบที่ผ่าน
- ✅ Backend unit suite: **76/76** (`npm run test:unit`, DB-free)
- ✅ **Integration suite: 62/62 suites — 641 passed, 2 skipped, 0 failed** (`npm run test:ci` กับ `lampang_bus_test`). 2 skipped = ops-readiness scripts ที่ต้อง prod-like env (gate ไว้ skip บน dev, รันจริงใน CI) + เพิ่ม fail-safe test
- ✅ Frontend build ผ่าน · Hybrid Motion UI guard ผ่าน · **Label checker: 0 violations** (แก้ 7 warning เดิมหมดแล้ว)

### 1.5 Commit (local เท่านั้น — ยังไม่ push)
```
d1b0025  docs: design system, progressive-deploy plan, API path sync, CI
97b44b1  feat(frontend): verification UI + hybrid-motion UX polish
45af988  feat(backend): vehicle verification, driver shifts, safety-policy, readiness + UAT fixes
9551b8b  chore(test): disposable lampang_bus_test isolation harness
```

### 1.6 สถานะระบบ local (กำลังรันอยู่)
- Backend `:3000` (โค้ดล่าสุด, ใช้ฐาน **`lampang_bus_dev_new`** ข้อมูลปัจจุบัน 245 คัน) + Vite `:5173` → เข้าใช้ที่ **http://localhost:5173**
- บัญชีทดสอบ (รหัส `demo123`): `demo_admin` `demo_province` `demo_aff` `demo_sch1` `demo_transport` `demo_drv` (seed ใน `lampang_bus_dev_new`)

---

## 2. ยังไม่ได้ทำ / รอดำเนินการ ⬜

### 2.1 ✅/🟡 Production — ไม่ต้อง restore (ปกติดี) + งานจริง = refresh local
- ✅ **ตรวจ prod ผ่าน Codex แล้ว (read-only):** prod ปกติ มี vehicles=245 (ใหม่กว่า backup 210), backend health OK, ไม่มี `Unknown database` → **restore ถูกยกเลิก** (gate `SHOW DATABASES` จับได้ว่าฐานมีอยู่ → ไม่แตะ = กันข้อมูลหาย)
- ✅ **dump สดจาก prod แล้ว (read-only):** `lampang_bus_20260623_051756.sql.gz` (648K, "Dump completed", sha256 `d48e2989195d193f212a80af04f1bbfc91fba30e359f768010041fff94010620`)
- ✅ **refresh local dev เสร็จแล้ว:** restore dump สด → ฐานใหม่ `lampang_bus_dev_new` (245 vehicles, apply 038/039 = 47 ตาราง) → seed `demo_*` → สลับ `backend/.env` ให้ใช้ฐานใหม่ + restart → login + vehicles=245 ผ่าน (ฐานเดิม `lampang_bus_dev` เก็บเป็น fallback)
- ✅ **cleanup prod เสร็จแล้ว:** DROP ฐาน scratch `lampang_bus_recovery_20260623` (verify `lampang_bus` ยังอยู่ 245 คัน ไม่ถูกแตะ)
- 🧹 **post-mortem (ไม่เร่งด่วน):** หาเหตุ `Unknown database` ชั่วคราวเมื่อ 06-22 ~17:53 (หายเองแล้ว)
- หมายเหตุ: `docs/PRODUCTION-RECOVERY-2026-06-23.md` (runbook กู้ prod) เก็บเป็น **contingency เผื่อกรณีฉุกเฉินจริง** เท่านั้น — สถานการณ์นี้ไม่ใช่

### 2.2 🟡 commit + เผยแพร่
- ✅ **Integration suite รันแล้ว: 641 passed / 2 skipped / 0 failed** (สร้าง `lampang_bus_test` + grant `lampang_dev` ใน WSL แล้ว)
- ⬜ **commit รอบใหม่** — งานหลัง 4 commits แรก (UX pages, ops-test fix, งาน Codex: operationsAlert/migration-baseline, docs) ยัง uncommitted; **รอพี่สั่ง "commit"** แล้วจัดเป็นกลุ่ม (backend ops/migration · frontend UX · docs)
- ⬜ **Phase 11A (Intelligent Tracking) ยังไม่ commit** — ETA + geofence + route deviation services/routes/frontend + migration 040 + nightly jobs + sidebar + UAT + user manual เขียนครบแล้ว รอสั่ง commit
- ⬜ **Push** — รอสั่งตรง ๆ
- ⬜ deploy ตามแผน progressive: migration 038/039 บน staging → OBSERVE → pilot ENFORCE → ทั้งจังหวัด
- ⬜ **Phase 11A deploy** — หลัง migration 040 บน staging → เปิด `FEATURE_ETA` ก่อน (low risk) → สังเกต 1 สัปดาห์ → ค่อยเปิด `FEATURE_GEOFENCE` → สังเกต → สุดท้าย `FEATURE_ROUTE_DEVIATION` (ต้องมี baseline ≥ 3 วัน)

### 2.3 ⚪ เลื่อนไว้ / ทำเพิ่มโดย Codex (harmonize เข้าเป็นทีมเดียวแล้ว)
- ✅ **operationsAlert webhook delivery** — Codex สร้าง `operationsAlert.service.js` (PII-redact + config-gated + bounded timeout) + wire เข้า `integrity-monitor.js` แทน stub เดิม
- ✅ **migration baseline validation** — Codex เพิ่ม `validate-migration-baseline.js` + `legacy-drift-baseline.json` + script `check:migrations` (ตรง deploy-plan migration-drift)
- ✅ **operationsHealth** — Codex เรียก `getDeploymentReadiness` (service ของ Claude) + แก้ correctness `dup_active_assignment` ให้ตรง migration 039
- ⬜ Readiness drill-down ราย รร./เขต (ตอนนี้ province-wide aggregate)
- ⬜ `GET /api/province/reports/policy` (รายงานเชิงนโยบาย) — ยังไม่ implement
- ⬜ DB integration test ใน CI (`full-quality.yml` รัน unit + build + labels; integration ต้องเพิ่ม MySQL service)

### 2.4 ⚪ House-keeping
- ⬜ แก้ 7 label warning เดิม (advisory)
- ⬜ บัญชี `demo_*` อยู่ใน `lampang_bus_dev_new` (ฐานที่ backend ใช้แล้ว) — เก็บไว้ลอง UI ได้
- ⬜ ลบฐาน `lampang_bus_dev` เก่า (210 คัน) เมื่อมั่นใจว่า `_new` โอเค
- ⬜ เปลี่ยนรหัส SSH prod (หลังคัดลอกฐานเสร็จ)
- ⬜ (prod) ใช้ pm2 ตาม `ecosystem.config.js` ให้ backend ทน reboot

---

## 3. ข้อควรระวัง (สำคัญ)
- **ฐาน local:** `lampang_bus_dev_new` (ใช้งานจริง 245 คัน/47 ตาราง), `lampang_bus_dev` (เก่า fallback), `lampang_bus_test` (disposable สำหรับเทสต์) — ห้ามรันคำสั่งทดสอบทำลายล้างนอก `lampang_bus_test` (Jest guard + `.env.test` ป้องกันแล้ว)
- บนเซิร์ฟเวอร์ prod: **prod ปกติดี ห้าม restore ทับ** (จะทำข้อมูลปัจจุบัน 245 คันหาย). บน prod ทำได้แค่อ่าน (`SHOW`/`mysqldump`) + cleanup ฐาน scratch; การเขียนฐานจริงต้องมีเหตุชัดเจน + operator ยืนยันก่อน
- git `commit`/`push`/`reset` จะทำต่อเมื่อได้รับคำสั่งระบุ action ชัดเจนเท่านั้น
