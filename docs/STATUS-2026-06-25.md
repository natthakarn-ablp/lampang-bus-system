# สถานะงาน — 25 มิถุนายน 2569 (2026-06-25)

**Branch:** `security/audit-fixes-2026-06-18`
**Production:** ทำงานปกติ, PM2 online, build ผ่าน

## งานที่เสร็จในรอบนี้

### 1. Role Inversion — กลับทิศบทบาทการขึ้นทะเบียนรถ (commit cc1cf93)
- **Backend:** `createDriverApplication`, `listDriverApplications`, `reviewApplication`
  - POST `/api/driver/applications` — คนขับยื่นคำขอเอง
  - GET `/api/driver/applications` — คนขับดูคำขอตัวเอง
  - POST `/verification/applications/:id/review` — โรงเรียนตรวจสอบ
  - GET `/verification/applications/:id/timeline` — audit trail สำหรับทุก role
- **Frontend:** `DriverApplications.jsx` (หน้าใหม่), `VehicleVerification.jsx` (เพิ่มปุ่มอนุมัติ/ปฏิเสธ), `MobileBottomNav.jsx` (เพิ่ม tab ขึ้นทะเบียน)
- **State machine:** PENDING_SCHOOL_REVIEW -> READY_TO_PRINT -> SUBMITTED -> INSPECTION_PENDING -> PASSED/FAILED/REJECTED

### 2. Tier 3 — กระจายงานแอดมินไปสังกัด (commit 0f00fbe)
- **Backend:** `listForAffiliation`, `getDetailForAffiliation` ใน studentTransfer.service.js และ vehicleRequest.service.js
  - 8 endpoints ใน affiliation.routes.js: transfer-requests + vehicle-requests (list, detail, approve, reject)
  - กรองตาม affiliation_id ทุก endpoint
- **Frontend:** `AffTransferRequests.jsx`, `AffVehicleRequests.jsx` (หน้าใหม่), Sidebar.jsx (เพิ่ม section คำขอและอนุมัติ)

### 3. อัปเดตคู่มือ
- `ADMIN_WORKFLOWS.md` — อัปเดต RBAC section ให้สะท้อนการกระจายงาน
- `user-guide-driver.html` — เพิ่มงานที่ 14 "ขึ้นทะเบียนรถ"
- `user-guide-affiliation.html` — เพิ่มงานที่ 13B/13C อนุมัติคำขอโอนย้าย/รถ

## สถานะ Production
- Backend: online, ไม่มี error ใน logs
- Frontend: build ผ่าน, deployed
- Database: ปกติ (245 คัน)
- ไม่ต้อง restore backup

## งานที่เหลือ
- ถ่ายภาพหน้าจอใหม่สำหรับคู่มือ (driver applications, affiliation approvals)
- UAT เชิงผู้ใช้กับ workflow ใหม่
