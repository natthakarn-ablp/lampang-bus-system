# บันทึกการ deploy production — 5 กันยายน 2569 ค่ำ (`c0b0d49` → `a0e783e`)

> รอบที่สองของวัน ต่อจาก `deploy-2026-09-05-c0b0d49.md` ทุกค่าคัดจาก output จริง
> รอบนี้ผู้ช่วยรันคำสั่งบน server เองผ่าน ssh (เครื่องมืออนุญาต) ตามสิทธิ์ที่เจ้าของระบบให้ไว้สำหรับงานเสี่ยงต่ำ

## 1. อะไรถูก deploy

| commit | เนื้อหา | ประเภทตามเกณฑ์ deploy อัตโนมัติ |
|---|---|---|
| `8c2eb4e` | `scripts/deploy-backend.sh` เขียนใหม่: fetch → ตรวจ branch/dirty → `git merge --ff-only` → บันทึก before/after → `npm ci` เมื่อ lockfile เปลี่ยน → syntax → unit → `pm2 reload` → health; ทุกขั้นที่ล้มหยุดก่อนแตะ PM2 (หมายเหตุ 2026-09-06: ข้อความนี้เกินจริงสำหรับ health check ซึ่งรันหลัง reload — เทสต์ `deployBackendScript.unit.test.js` กรณี health ล้มเองก็ assert ว่า reload เกิดแล้ว; แก้ข้อความและเพิ่ม rollback ใน `eba7c4a`) | operational tooling ที่ทดสอบได้ (`backend/tests/deployBackendScript.unit.test.js` 12 scenario กับ repo ทิ้ง) |
| `d1449dc` | frontend: `ExecutiveSummary.jsx`, `ExecutivePrint.jsx`, `ResearchMetrics.jsx` ใช้ helper กลาง `utils/kpi.js` — ตัวส่วน 0/ไม่มี → `null` แสดง "ไม่มีข้อมูล" ไม่นับเป็นดีขึ้น/ลดลง/ต่ำ ตรงกับ backend export | presentation/calculation ให้ตรง contract เดิม (`backend/tests/frontendSnapshotPctContract.unit.test.js` 40 ข้อ + ตรวจในเบราว์เซอร์ headless desktop/mobile/print) |
| `a0e783e` | deploy script: ไฟล์ untracked แจ้งแต่ไม่ขวาง, log ประวัติย้ายไป `/home/schoolbus/logs/deploy-history.log` | แก้จากการรันจริงรอบแรก (ข้อ 3.4) |

ไม่มี: migration, dependency ฝั่ง backend, feature flag, การเขียน `lampang_bus`, การเปลี่ยน auth/RBAC/infra

## 2. เกณฑ์ที่ตรวจก่อน deploy

- CI `Full Quality` เขียวที่ `8c2eb4e` และ `d1449dc` (ทั้ง `test:unit` และ `test:ci` กับ MySQL service) ส่วน `a0e783e` ยังอยู่ระหว่างรันขณะ deploy — เป็นการแก้สคริปต์ deploy เองซึ่งการรันบน server คือการทดสอบ และผ่านในเครื่องนี้ 17/17
- Diff จำกัดขอบเขต: 2 สคริปต์/เทสต์ + 4 ไฟล์ frontend + 1 helper ไม่มีงานค้างของคนอื่นปน (worktree สะอาดทั้งสองฝั่ง)
- Rollback: backend = commit ก่อนหน้า `c0b0d49` (บันทึกใน deploy-history.log) · frontend = สำเนา `dist` เดิมที่ `/home/schoolbus/backups/frontend-dist/dist.prev-20260905-145906` (4.2 MB)
- เวลา: 21:58–22:04 น. วันเสาร์ ไม่มีรถรับส่ง ผู้ใช้ไม่กระทบ (`pm2 reload` แบบไม่ดับบริการ; frontend เป็น static)

## 3. ผลจริง (เวลา UTC บน server)

| ขั้น | ผล |
|---|---|
| 3.1 ตรวจก่อน (14:58) | head `c0b0d49` · dirty 0 · pm2 online restarts 19 · RAM available 1,071 MB · ดิสก์ว่าง 26 GB · `/health` 200 |
| 3.2 pull (ff-only) | `c0b0d49..d1449dc` · backend dep/migration diff ว่าง · frontend 4 ไฟล์ · พบว่า `frontend/node_modules` (25 ส.ค.) เก่ากว่า `frontend/package-lock.json` (4 ก.ย. `307c28e`) |
| 3.3 frontend | สำรอง `dist` → `npm ci --no-audit --no-fund` (220 packages, 5 s) → `npm run build` (vite 8.2.2, 3 s) → `dist/index.html` 14:59 · assets 150 ไฟล์ · symlink `dist/manual`, `dist/docs` ถูกสร้างใหม่ · chunk `ExecutiveSummary-DmoDqF4o.js` มีข้อความใหม่ "ยังเทียบกับ baseline ไม่ได้" |
| 3.4 deploy script รอบแรก (`d1449dc`) | **ABORT ถูกต้องตามที่ออกแบบ** — สคริปต์เห็น `frontend/dist.prev-*/` เป็น untracked แล้วหยุดก่อนแตะ PM2 · แต่ชี้บั๊กสองข้อของสคริปต์ใหม่: ไฟล์ untracked ไม่ควรขวาง และ log ที่เขียนใน repo จะขวางรอบถัดไปเอง → แก้เป็น `a0e783e` (ตรวจเฉพาะ tracked ที่แก้; log ย้ายออกนอก repo) ทดสอบ 17/17 แล้ว push |
| 3.5 pull รอบสอง | ย้าย `dist.prev-*` ไป `/home/schoolbus/backups/frontend-dist/` · dirty 0 · ff `d1449dc..a0e783e` |
| 3.6 deploy script (`a0e783e`) | "already up to date" → syntax ผ่าน → unit **75 suites / 979 tests ผ่าน** (37.9 s) → `pm2 reload` ✓ → `Health check OK — running a0e783e` · `deploy-history.log` มีบรรทัด `before=a0e783e after=a0e783e` (เพราะ pull ทำแยกก่อน; การเปลี่ยน runtime จริงคือ `c0b0d49` → `a0e783e`) |
| 3.7 ตรวจหลัง (15:04) | `/health` commit `a0e783e` uptime 30 s · restarts **20** · log: `Shared security state check passed` + `running on` 15:03:54 · ไม่มี FATAL/no such table · login probe **401** · `capacity-sample` ไม่มี token **401** · เว็บสาธารณะ `/` 200, `/manual/` 200, chunk ใหม่ 200 และมีข้อความใหม่ 1 แห่ง |

## 4. หลักฐานที่สร้างใหม่ที่ `a0e783e`

| | ค่า |
|---|---|
| go-live bundle | `outputs/go-live-bundle/20260905-220457` git_head `a0e783e` (allow-pending; PASS pending allowed) |
| closure status | `outputs/go-live-closure-status/20260905-220457` |
| automated readiness | `outputs/automated-readiness/20260905-220458` — **pass=5 pending=9 fail=0** · local gate 14/14 · public gate 5/5 บน runtime ใหม่ |
| RBAC matrix / scope audit | `outputs/rbac-matrix/20260905-215630` — ดู `docs/audit/menu-baseline-2026-09-04.md` §12 (re-audit) |
| UAT live check (staging สังเคราะห์) | `outputs/uat-live-check/20260905-215608` — pass=28 fail=0 (7 บทบาท × login + 3 route) บน `lampang_bus_staging` ไม่ใช่ production |

ตอนนี้ origin = production runtime = bundle = closure = readiness = **`a0e783e`** (จนกว่าจะ commit เอกสารชุดนี้ ซึ่งเป็น docs-only) — หมายเหตุ 2026-09-06: commit หลัง `a0e783e` บน branch นี้ (`536bf1b`, `19a74c9`, `8cb7d8a`, `2fc67d1`) เป็น docs/scripts **และ tests** (`19a74c9` แก้ `backend/scripts/load-test.js`, `scripts/restore-drill-db.sh` พร้อมเทสต์) ไม่ได้ deploy

## 5. ผลข้างเคียงและสิ่งที่ยังค้าง

- login probe สร้างแถว `login_lockouts` ให้ `__probe` อีก 1 ครั้ง (fail_count สะสม 2) — cron 04:00 ล้าง
- `frontend/node_modules` บน server ถูกติดตั้งใหม่ตาม lockfile ปัจจุบัน — เป็นครั้งแรกตั้งแต่ 25 ส.ค.
- ส่วนที่ล้นขอบขวาบนหน้าจอกว้าง 390 px (headless) เกิดกับหน้าที่ไม่ได้แก้ด้วย ไม่ใช่จาก release นี้ ยังไม่ได้ตรวจบนอุปกรณ์จริง
- ยังไม่มี: migration 050, flag ใด ๆ, ใบอนุมัติเป็นลายลักษณ์อักษร (ดู `retrospective-attestation-2026-09-05.md`)
