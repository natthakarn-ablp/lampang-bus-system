# บันทึกการ deploy production — 5 กันยายน 2569 (`208e883` → `c0b0d49`)

> เขียนทันทีหลังทำ ทุกค่าคัดจาก output จริงของคำสั่งบน server ไม่ได้คาดเดา
> รูปแบบเดียวกับ §7 ของ `deploy-runbook-051-shared-security-state.md` เพื่อให้บันทึกปิดโครงการอ่านต่อกันได้

## 1. อะไรถูก deploy และอะไรไม่ถูกแตะ

| | |
|---|---|
| commit ที่ deploy | `c0b0d49` (`feat/tracking-security-hardening`) — 8 commits จาก handoff §2 ข้อ 1–9 รวมเอกสาร |
| โค้ด runtime ที่เปลี่ยน | `backend/src/index.js` (ด่าน boot 050, no-op เมื่อ flag ปิด), `backend/src/routes/admin.routes.js` (research export + endpoint `operations/capacity-sample`), `backend/src/services/capacitySample.service.js`, `backend/src/utils/researchSnapshotFields.js` — ทั้งหมดอยู่ใต้ `/api/admin/*` |
| dependency / migration | **ไม่มี** — `git diff --stat 208e883..c0b0d49 -- backend/package.json backend/package-lock.json backend/migrations` ว่าง จึงไม่มี `npm ci` และไม่มีขั้น DB |
| **ไม่ได้ทำ** | apply migration 050 · เปลี่ยน feature flag ใด ๆ · build/deploy frontend (ไม่มีไฟล์ใน `frontend/src` เปลี่ยน) · เขียนข้อมูลใด ๆ ลง `lampang_bus` |

**ที่มาของการอนุมัติ:** เจ้าของโครงการอนุมัติในเซสชันเดียวกัน (ข้อความ: "แบ็กอัปข้อมูลและแก้ไขให้มีความเสี่ยงต่ำมากที่สุด และอนุมัติให้ทำการ deploy โปรดักชั่นได้เลย")
โดยมีเงื่อนไขว่าต้อง backup ก่อนและลดความเสี่ยงต่อข้อมูลให้ต่ำสุด — จึง deploy เฉพาะโค้ดและไม่แตะ schema/flag
`docs/PHASE9_OWNER_OPERATOR_APPROVAL_2026-08.md` ยังว่างเหมือนเดิม (ผู้ช่วยไม่กรอกเอกสารอนุมัติ) — การบันทึกย้อนหลังเป็นงานของเจ้าของโครงการ เช่นเดียวกับที่ handoff §0.2 ระบุสำหรับ 051

**วิธีรัน:** คำสั่ง ssh ของผู้ช่วยถูก classifier ของเครื่องมือบล็อก ผู้ปฏิบัติจึงรันคำสั่งที่ผู้ช่วยเตรียมให้ทีละขั้นผ่าน `! ssh ssh.schoolbus.lp-pao.go.th '…'`
ในเซสชันเดียวกัน แล้วผู้ช่วยอ่านผลก่อนอนุญาตขั้นถัดไป คำสั่งทุกขั้นอยู่ใน §3

## 2. ผลจริง

เวลาเซิร์ฟเวอร์: 13:38–13:40 UTC = **20:38–20:40 น. วันเสาร์ตามเวลาไทย** — ไม่มีรถรับส่ง ไม่มีเช็กอิน

| ขั้น | ผล |
|---|---|
| 0 ตรวจก่อน (อ่านอย่างเดียว) | head `208e883` · worktree สะอาด · pm2 online restarts **18** · RAM available 1,057 MB / 1,967 MB · ดิสก์ว่าง 26 GB · backup cron ล่าสุด `lampang_bus_20260905_023001.sql.gz` (2.67 MB) · `/health` 200 commit `208e883` · Node **v20.20.2** |
| 1 backup ใหม่ | `scripts/backup-db.sh` → `lampang_bus_20260905_133913.sql.gz` 2,673,855 bytes + `.sha256` · `scripts/verify-latest-backup.sh` → gzip OK · sha256 OK · mysqldump markers OK · **RESULT: PASS** · retention ลบ dump เก่ากว่า 7 วัน 1 ไฟล์ (พฤติกรรมปกติของ cron) |
| 2 pull | `git pull --ff-only` → `Fast-forward 208e883..c0b0d49` 40 ไฟล์ (+2,904 / −65) · head `c0b0d49` · dep/migration diff ว่าง |
| 3 deploy (`scripts/deploy-backend.sh`) | syntax check ผ่าน → unit **73 suites / 922 tests ผ่าน** (เมื่อเช้า 67 / 811; +6 suites / +111 tests = เทสต์ใหม่ทั้งหมดของงานนี้) → `pm2 reload` ✓ → `[deploy] Health check OK` |
| 4 ตรวจหลัง | `/health` commit **`c0b0d49`** uptime 24 s · restarts **19** · log: `Shared security state check passed (migration 051 tables present)` + `running on 127.0.0.1:3000` ที่ 13:40:16 UTC · ไม่มี `FATAL` / `no such table` · `POST /api/auth/login` (บัญชีไม่มีจริง `__probe`) → **401** ไม่ใช่ 500 · `GET /api/admin/operations/capacity-sample` ไม่มี token → **401** (route มีและถูกกันสิทธิ์) · `GET /api/admin/research-export` ไม่มี token → **401** |

ผลข้างเคียงที่รู้: การยิง login ทดสอบสร้างแถว `login_lockouts` ให้ `__probe` 1 แถว (`fail_count=1`) ตามการออกแบบ 051 — **ไม่ได้ลบ** เพราะไม่แตะ DB ในรอบนี้ cron ล้างตาราง 04:00 จะเก็บเอง audit row ของ EXPORT/login ไม่มี (ไม่มีคำขอที่ผ่าน auth)

## 3. คำสั่งที่รันจริง (ตามลำดับ)

```bash
# 0 ตรวจก่อน
ssh ssh.schoolbus.lp-pao.go.th 'cd /home/schoolbus/apps/lampang-bus-system && echo "branch=$(git branch --show-current) head=$(git rev-parse --short HEAD) dirty=$(git status --short | wc -l)" && pm2 list && free -m | head -2 && df -h /home/schoolbus | tail -1 && ls -lt /home/schoolbus/backups/lampang-bus/*.sql.gz | head -2 && curl -s --max-time 5 http://127.0.0.1:3000/health'
# 1 backup + verify
ssh ssh.schoolbus.lp-pao.go.th 'cd /home/schoolbus/apps/lampang-bus-system && bash scripts/backup-db.sh && bash scripts/verify-latest-backup.sh'
# 2 pull แบบ fast-forward แยกจากสคริปต์ deploy (สคริปต์ใช้ git pull || true ซึ่งกลืน error)
ssh ssh.schoolbus.lp-pao.go.th 'cd /home/schoolbus/apps/lampang-bus-system && git fetch origin && git pull --ff-only origin feat/tracking-security-hardening && echo "head=$(git rev-parse --short HEAD)" && git diff --stat 208e883..HEAD -- backend/package.json backend/package-lock.json backend/migrations | tail -1'
# 3 deploy
ssh ssh.schoolbus.lp-pao.go.th 'cd /home/schoolbus/apps/lampang-bus-system && bash scripts/deploy-backend.sh 2>&1 | tail -40'
# 4 ตรวจหลัง
ssh ssh.schoolbus.lp-pao.go.th 'curl -s http://127.0.0.1:3000/health; pm2 list | grep schoolbus-backend; pm2 logs schoolbus-backend --lines 80 --nostream | grep -iE "fatal|error|no such table|migration|check passed|running on" | tail -12; curl -s -o /dev/null -w %{http_code} -X POST -H "Content-Type: application/json" -d "{\"username\":\"__probe\",\"password\":\"x\"}" http://127.0.0.1:3000/api/auth/login'
```

## 4. หลักฐานที่สร้างใหม่หลัง deploy ให้ชี้ commit เดียวกับ runtime

| | ค่า |
|---|---|
| go-live bundle | `outputs/go-live-bundle/20260905-204108` git_head `c0b0d49` · validator PASS (pending allowed) pass=2 pending=7 fail=0 |
| closure status | `outputs/go-live-closure-status/20260905-204109` source_git_head `c0b0d49` · PENDING actions=20 pass=1 pending=9 fail=0 |
| automated readiness | ดู §5 |

ตอนนี้ origin = local (ก่อน commit เอกสารนี้) = production runtime = bundle = closure = **`c0b0d49`** เหลือที่ยังไม่ตรงคือ pin ของ menu baseline / residual-risk (`4b80b4b`) ซึ่งต้อง re-audit ไม่ใช่แก้ตัวเลข (ดู `docs/project-closure/commit-alignment-2026-09-05.md` §2) — และ HEAD จะนำหน้า runtime หนึ่ง commit ที่เป็นเอกสารนี้ ซึ่งเป็นสภาพเดียวกับทุกครั้งที่ผ่านมา

## 5. automated readiness ที่ `c0b0d49`

รันจากเครื่องพัฒนา 20:42 น. ด้วย `node scripts/collect-automated-readiness-evidence.js --bundle outputs/go-live-bundle/20260905-204108 --closure outputs/go-live-closure-status/20260905-204109` (ยิง HTTP อ่านอย่างเดียวไปที่ `https://schoolbuslampang.com` ตาม default ของสคริปต์ ไม่มี DB write)

| | ค่า |
|---|---|
| output | `outputs/automated-readiness/20260905-204214` · git_head **`c0b0d49`** · worktree clean |
| สรุป | **pass=5 pending=9 fail=0** · human actions 7 |
| local-gate | PASS — `[gate] summary pass=14 warn=0 fail=0 skip=0` (unit test, npm audit backend+frontend, frontend build, labels, hybrid-ui) |
| public-gate | PASS — `[gate] summary pass=5 warn=0 fail=0 skip=0` (ยิงเว็บ production หลัง deploy) |
| git-status / secret-scan (head, staged) | PASS |
| pending 9 รายการ | phase9-evidence (ไม่ได้ส่ง `--phase9-evidence` เพราะชุดเมื่อเช้าเก็บบน server ไม่มีบนเครื่องนี้), uat-evidence ×2, restore-drill, operator-gates, go-live-signoff (ok=8 pending=119), readiness-100, bundle-structure, closure-structure — ทั้งหมดเป็นหลักฐานหรือลายเซ็นจากคน |

เทียบกับรอบเช้า (`5a6a2d1`: pass=4 pending=10 fail=0) — ดีขึ้น 1 รายการจาก public-gate ที่วิ่งบน runtime ใหม่ ไม่มีรายการใดแย่ลง

## 6. สิ่งที่ยังค้าง

- migration 050 และ flag ทุกตัว: ตัดสินใจแยก ทีละรายการ หลัง C0-4 / C0-7 (`commit-alignment-2026-09-05.md` §4)
- เอกสารอนุมัติ `PHASE9_OWNER_OPERATOR_APPROVAL_2026-08.md` ยังว่าง 9 ช่อง — การ deploy รอบนี้และ 051 เมื่อเช้าควรถูกบันทึกย้อนหลังโดยเจ้าของโครงการ
- ~~`deploy-backend.sh` ยังใช้ `git pull || true`~~ **แก้แล้วในรอบถัดมา** (commit ที่แก้ `scripts/deploy-backend.sh` หลัง `d889530`): fetch → ตรวจ branch/dirty → `git merge --ff-only` → บันทึก before/after ลง `deploy-history.log` → `npm ci` เฉพาะเมื่อ lockfile เปลี่ยน → syntax → unit → `pm2 reload` → health; ทุกขั้นที่ล้มหยุดก่อนแตะ PM2 พิสูจน์ด้วย `backend/tests/deployBackendScript.unit.test.js` (repo ทิ้ง + stub pm2/npx/npm/curl) รอบนี้ยังคงเลี่ยงด้วยการ pull แยกก่อนตามที่บันทึกไว้
