# บันทึกการ deploy production — 7 กันยายน 2569 เช้า (`14caf5b` → `f60aee5`)

> รอบที่สี่ ต่อจาก `deploy-2026-09-06-14caf5b.md` ทุกค่าคัดจาก output จริงของคำสั่งบน server
> ผู้ช่วย (Claude Code) รันคำสั่งบน server เองผ่าน ssh ตามคำสั่งของเจ้าของระบบในเซสชันเดียวกัน (7 ก.ย. 2569 "เผยแพร่ขึ้นเว็บจริงเลย")
> เวลาในเอกสาร: UTC บน server = เวลาไทย −7 ชม.

## 1. อะไรถูก deploy และอะไรไม่ถูกแตะ

| commit | เนื้อหา | ประเภท |
|---|---|---|
| `46511f3` | ภาพประกอบคู่มือ 76 ใบถ่ายใหม่ทั้งชุด + ซ่อมชุดถ่ายภาพ (`scripts/ui-redesign/capture.mjs`, `manual-screenshots.mjs`): ส่ง feature flag ตามที่ production เปิดจริง, fixture `/api/readiness` ตรง shape ของ service, fixture เหตุฉุกเฉินมี `meta`, ด่านกันภาพ ErrorBoundary และภาพซ้ำ · แก้หน้าเหตุฉุกเฉิน 3 หน้าให้ไม่ดับทั้งหน้าเมื่อคำตอบไม่มี `meta` · เทสต์ใหม่ `backend/tests/manualScreenshotHarness.unit.test.js` | presentation + tooling + tests |
| `4e1d2d5` | ข้อความคู่มือทุกฉบับตรวจกับโค้ดแล้วแก้: ชื่อช่อง login, ตัวเลขล็อกบัญชี/เครือข่าย, กฎรหัสผ่าน, ถอดคำอ้างอายุเซสชัน, ตารางฟีเจอร์ครบ 10 รายการ, ป้าย historical ให้ฉบับที่ไม่ได้ดูแล · **ถอดรหัสผ่านตั้งต้นที่เคยพิมพ์ไว้ในคู่มือสาธารณะ 4 ฉบับ** · สร้าง PDF ใหม่ 19 ไฟล์ | docs |
| `f60aee5` | สลับภาพคนขับให้ภาพที่คู่มืออ้างเป็นหน้าจอโหมดเลือกทีละคนตามคำบรรยาย | docs/tooling |

**ไม่มี:** migration · การเปลี่ยน `backend/package.json` / `package-lock.json` · feature flag · การเขียน `lampang_bus` (ไม่มี login probe) · การเปลี่ยน auth/RBAC/infra — `git diff --stat 14caf5b..f60aee5 -- backend/src backend/package.json backend/package-lock.json backend/migrations ecosystem.config.js` **ว่าง**

**มี:** `frontend/src` เปลี่ยน 3 ไฟล์ (`school/EmergencyList.jsx`, `affiliation/AffEmergencyList.jsx`, `province/ProvEmergencyList.jsx` — ค่าสำรองของ `meta`) จึงต้อง build frontend ใหม่ · `backend/tests` เพิ่ม 1 ไฟล์ (ไม่ใช่โค้ดที่รัน)

## 2. เกณฑ์ที่ตรวจก่อน deploy (เครื่องพัฒนา Windows, Node v24.15.0)

| การตรวจ | ผล |
|---|---|
| ถ่ายภาพคู่มือทั้งชุด | 76/76 ผ่านด่าน ErrorBoundary และด่านภาพซ้ำ exit 0 |
| สร้าง PDF | `build-manual-pdf.sh` 8 ไฟล์ + `build-training-manual-pdfs.py` 11 ไฟล์ สำเร็จ |
| jest (คู่มือ + frontend contract) | 5 suites / 99 tests ผ่าน |
| ลิงก์และภาพในคู่มือ | 199 relative links + 96 training links — เสีย 0 (18 รายการที่เครื่อง Windows รายงานเป็น symlink ของ git ที่ resolve บน server) |
| `npm run build` (frontend) | ผ่าน |
| secret scan บน diff | 0 hit |

## 3. Preflight บน server (อ่านอย่างเดียว, 00:35–00:37 UTC)

`HEAD 14caf5b` สะอาด · `/health` commit `14caf5b` db connected uptime 22.8 ชม. · pm2 online restarts 21 mem 93 MB · RAM available 1,186 MB · ดิสก์ว่าง 26 GB · การเชื่อมต่อ :3000 = **0**

**การใช้งานจริงขณะ deploy (เวลาไทย จันทร์ 7 ก.ย. 07:36 น.):** aggregate อ่านอย่างเดียวจาก `lampang_bus` — เช็กชื่อวันนี้ **0** รายการ · เช็กชื่อ 1 ชม.ล่าสุด **0** · audit log 1 ชม.ล่าสุด **0** · login 24 ชม. 27 ครั้ง — ตรงกับที่ Phase 12 (rollout) ยังไม่เริ่ม (กำหนด พ.ย. 2569) จึงตัดสิน GO แม้เป็นเช้าวันจันทร์

| ขั้น | ผล |
|---|---|
| S1 backup + verify (00:37:10–11) | `lampang_bus_20260907_003710.sql.gz` + `.sha256` · gzip OK · sha256 OK · markers OK · **RESULT: PASS** (12 ชุดในโฟลเดอร์) |
| S2 pin + วางสคริปต์นอก checkout | `git fetch` → `FETCH_HEAD = f60aee5585119192e92b4ba01e7bfb531080ed75` ตรงกับรุ่นที่ตั้งใจ · `merge-base --is-ancestor` ผ่าน · `git show <R>:scripts/deploy-backend.sh` → sha256 `6a35438a…` **ตรงกับเครื่องพัฒนา** · `bash -n` ผ่าน |
| S3 deploy (00:41:58–00:42:48) | ff `14caf5b..f60aee5` → rollback_ref `14caf5b(running)` → marker ตรง lockfile จึง **ไม่ต้อง `npm ci`** (`deps=untouched`) → syntax ผ่าน → **jest 80 suites / 1,091 tests ผ่าน 41.6 s** → `pm2 reload` ✓ → **`Health check OK — running f60aee5 (deployed f60aee5, was 14caf5b), database connected`** |
| S4 frontend (00:43:02–00:43:30) | สำรอง `dist` → `/home/schoolbus/backups/frontend-dist/dist.prev-20260907-004302` (4.2 MB) · `npm ls` ไม่พบปัญหา · `npm run build` ✓ 3.17 s · symlink `manual`/`docs` สร้างใหม่ · `dist/index.html` 00:43 |
| ตรวจหลัง (00:43:57) | `/health` commit `f60aee5` db true · pm2 online **restarts 22** mem 76 MB · history: `end result=ok head=f60aee5 deps=untouched exit=0` |
| smoke สาธารณะ (ไม่เขียนข้อมูล) | `/` 200 · `/manual/` 200 (33,955 B) · `/manual/user-guide-driver.html` 200 (92,516 B) · `/manual/user-guide-province.html` 200 (82,308 B) · `/manual/pdf/driver.pdf` 200 (2,994,728 B — PDF ใหม่) · `/manual/screenshots/driver/15-roster.png` 200 · `/api/auth/me` ไม่มี token → **401** · index อ้าง chunk ใหม่ `index-B87wIqbv.js` (เดิม `index-Jo3JSBLm.js`) |

## 4. เฝ้าดูหลัง deploy 15 นาที

ตัวอย่าง 15/15 · ทุก 60 s · 2026-09-07T00:45:38 → 00:59:28 UTC · ทุกตัวอย่างเป็นคำสั่ง ssh อ่านอย่างเดียว

| ค่า | ผลตลอดช่วง |
|---|---|
| `/health` commit | `f60aee5` ทุกตัวอย่าง |
| `/health` database.connected | true ทุกตัวอย่าง |
| PM2 | online ทุกตัวอย่าง · restarts คงที่ 22 |
| บรรทัดใน error log | 0 (ไม่เพิ่ม) |
| RSS ของ process | 75–78 MB |
| การเชื่อมต่อค้าง :3000 | 0 |
| uptime ตัวอย่างสุดท้าย | 1,002 s |

**สรุป:** เสถียรตลอด 15 นาที (ตามนิยามนี้) — ไม่ใช่การเฝ้าระวังระยะยาวและไม่ใช่การทดสอบภาระ

## 5. ทางถอยกลับ (ตรวจแล้วว่ามีอยู่จริง; ไม่ได้ใช้)

- **backend:** commit `14caf5b` — worktree สะอาด `git reset --hard 14caf5b19f7e7b2e8a156db8ebbe22b35c6b5eaa && pm2 reload ecosystem.config.js` (ไม่ต้องสลับ node_modules เพราะรอบนี้ `deps=untouched`)
- **frontend:** `cp -a /home/schoolbus/backups/frontend-dist/dist.prev-20260907-004302/. frontend/dist/` (สำเนาก่อน build 00:43 UTC)
- **ฐานข้อมูล:** ไม่มีการเปลี่ยน schema/ข้อมูล · สำรองล่าสุด `lampang_bus_20260907_003710.sql.gz` (verify PASS)

## 6. สิ่งที่ยังค้าง

- ใบอนุมัติเป็นลายลักษณ์อักษร `PHASE9_OWNER_OPERATOR_APPROVAL_2026-08.md` (ตอนนี้ค้าง 4 รอบ: 5 ก.ย. ×2, 6 ก.ย., 7 ก.ย.), maintenance window (C0-8), ชื่อผู้รับผิดชอบ (C0-7) — ยังว่าง การอนุมัติรอบนี้อยู่ในเซสชันของเจ้าของระบบ
- หมายเหตุในหัวคู่มือเขียนว่า "ตรวจกับระบบจริง commit `14caf5b`" ซึ่งถูกต้องตามตอนที่ตรวจ แต่ระบบที่รันอยู่ตอนนี้คือ `f60aee5` (ต่างกันเฉพาะหน้าเหตุฉุกเฉิน 3 หน้าและเอกสาร) — แก้ในรอบเอกสารถัดไป ไม่ต้อง deploy เพื่อการนี้
- ภาพประกอบคู่มือถ่ายจาก fixture สังเคราะห์บนเครื่องพัฒนา ไม่ใช่ภาพจากระบบจริงที่มีข้อมูลจริง (ตามข้อกำหนด PDPA) — ระบุไว้ในคู่มือทุกฉบับแล้ว
