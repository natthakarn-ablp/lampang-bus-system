# Frontend production env (Phase 10.3E-UX1)
# Loaded by Vite at build time. Values must NOT be sensitive — anything in
# import.meta.env.VITE_* gets baked into the bundle and is publicly visible.

# LIFF app id for the parent linking page. Same id as backend LINE_LIFF_ID.
VITE_LIFF_ID=2009055277-UFhJvGve
